{% extends 'base.html.twig' %}

{% block title %}Skin{% endblock %}

{% block main_class %}main_container{% endblock %}

{% block main %}
    {% include('partials/_adminSideMenu.html.twig') %}

    <div class="admin_zone">
        <h1>Skin</h1>

        <a href="{{ path('app_admin_skin_index') }}">back to list</a>
        <a href="{{ path('app_admin_skin_edit', {'id': skin.id}) }}">edit</a>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skin.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ skin.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ skin.url }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ skin.type }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('app_admin_skin_index') }}">back to list</a>
        <a href="{{ path('app_admin_skin_edit', {'id': skin.id}) }}">edit</a>

        {{ include('admin_skin/_delete_form.html.twig') }}
    </div>
{% endblock %}
