<form method="post" action="{{ path('app_admin_cursed_object_delete', {'id': cursed_object.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cursed_object.id) }}">
    <button class="btn">Delete</button>
</form>
