{% extends 'base.html.twig' %}

{% block title %}OptionalGoal{% endblock %}

{% block main_class %}main_container{% endblock %}

{% block main %}
    {% include('partials/_adminSideMenu.html.twig') %}
    <div class="admin_zone">
        <h1>Optional Goal</h1>

        <a href="{{ path('app_admin_optional_goal_index') }}">back to list</a>
        <a href="{{ path('app_admin_optional_goal_edit', {'id': optional_goal.id}) }}">edit</a>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ optional_goal.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ optional_goal.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ optional_goal.description }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('app_admin_optional_goal_index') }}">back to list</a>
        <a href="{{ path('app_admin_optional_goal_edit', {'id': optional_goal.id}) }}">edit</a>

        {{ include('admin_optional_goal/_delete_form.html.twig') }}
    </div>
{% endblock %}
