<form method="post" action="{{ path('app_admin_optional_goal_delete', {'id': optional_goal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ optional_goal.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
