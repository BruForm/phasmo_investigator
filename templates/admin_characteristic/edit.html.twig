{% extends 'base.html.twig' %}

{% block title %}Edit Characteristic{% endblock %}

{% block main_class %}main_container{% endblock %}

{% block main %}
    <div class="admin_zone">
        <h1>Edit Characteristic</h1>

        <a href="{{ path('app_admin_characteristic_index') }}">back to list</a>

        {{ include('admin_characteristic/_form.html.twig', {'button_label': 'Update'}) }}

        {{ include('admin_characteristic/_delete_form.html.twig') }}

        <a href="{{ path('app_admin_characteristic_index') }}">back to list</a>
    </div>
{% endblock %}
