{% extends 'base.html.twig' %}

{% block title %}Characteristic{% endblock %}

{% block main_class %}main_container{% endblock %}

{% block main %}
    <h1>Characteristic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ characteristic.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ characteristic.type }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ characteristic.name }}</td>
            </tr>
            <tr>
                <th>description</th>
                <td>{{ characteristic.description }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ characteristic.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_characteristic_index') }}">back to list</a>

    <a href="{{ path('app_admin_characteristic_edit', {'id': characteristic.id}) }}">edit</a>

    {{ include('admin_characteristic/_delete_form.html.twig') }}
{% endblock %}
